"use strict";

// ----------calculator---------------------------------
// let userInput;
// let initialValue = 0;
// let iteration = 0;

// do {
//   userInput = prompt(
//     `Current result ${initialValue}\nEnter operation in format <operand><number>`
//   );
//   if (userInput !== null && userInput !== "") {
//     const operation = userInput.slice(0, 1);

//     if (
//       operation === "+" ||
//       operation === "-" ||
//       operation === "/" ||
//       operation === "*"
//     ) {
//       const number = Number(userInput.slice(1, userInput.length));

//       if (isNaN(number)) {
//         alert("You entered the wrong number");
//       } else {
//         let result;

//         if (operation === "+") {
//           result = initialValue + number;
//         } else if (operation === "-") {
//           result = initialValue - number;
//         } else if (operation === "/") {
//           result = initialValue / number;
//         } else if (operation === "*") {
//           result = initialValue * number;
//         }

//         //  alert("Result operation 0" + operation + number + "=" + initialValue);
//         alert(
//           `Result operation ${initialValue} ${operation} ${number} = ${result}`
//         );
//         initialValue = result;
//       }
//     } else {
//       alert("operation not avialable");
//     }
//   }

//   //   break;
//   iteration += 1; // делает так,что бы калькулятор не выполнял более указанной операции в  while (iteration < 2)
// } while (userInput !== null && userInput !== "" && iteration < 2);
// alert("calculator is finished");

// ----------------------------------------------------------------------
// console.log("Числа кратные 3");
// for (let i = 0; i < 100; i += 1) {
//   if (i % 3 == 0) {
//     console.log(i);
//   }
// }

// console.log("finish");

// ----zzzzzzzzzzzzzz----------------zzzzzzzzzzzzzzzzzzz
// -----------------------again-------------------------

