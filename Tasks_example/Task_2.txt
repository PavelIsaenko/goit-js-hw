2. // Выполняем до тех пор пока не равно = null и введено число 10
let userInput;

do {
  userInput = prompt("Введите число 10:");
  console.log(userInput);
} while (userInput !== null && Number(userInput) !== 10);