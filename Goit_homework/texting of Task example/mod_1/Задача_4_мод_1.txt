«адание 4
Ќа счету пользовател€ есть 23580 кредитов, значение хранитс€ в переменной credits (создай и присвой). ѕользователь решает купить ремонтных дроидов, которые сто€т по 3000 кредитов за штуку. ÷ена одного дроида хранитс€ в переменной pricePerDroid (создай и присвой).

ѕри посещении страницы, использу€ prompt, необходимо спросить количество дроидов которые пользователь хочет купить и сохранить в переменную.

Ќапиши скрипт который:

≈сли в prompt была нажата кнопка Cancel, выводит в консоль сообщение 'ќтменено пользователем!'.
¬ противном случае, рассчитывает общую цену заказа и сохран€ет в переменной totalPrice.
ѕровер€ет сможет ли пользователь оплатить заказ:
если сумма к оплате превышает количество кредитов на счету, выводи в консоль сообщение 'Ќедостаточно средств на счету!'.
в противном случае необходимо посчитать остаток кредитов на счету и вывести сообщение '¬ы купили [число] дроидов, на счету осталось [число] кредитов.'.

========================================================================================

const credits = 23580;
const pricePerDroid = 3000;
let message = prompt(" акое количество дроидов ¬ы хотите купить?");
// message = Number(message);
const totalPrice = message * pricePerDroid;
let counter;
let lost;
if (message === null) {
  console.log("ќтменено пользователем!");
} else if ((counter = totalPrice / 3000) * 3000 < credits) {
  message = Number(message);
  console.log(
    `“ы можешь купить ${counter} ƒроидов на сумму ${totalPrice}, и у теб€ еще осталось ${credits -
      totalPrice}`
  );
} else if ((lost = (counter = totalPrice / 3000) * 3000) > credits) {
  message = Number(message);

  console.log(
    `»звини друг, но ты не можешь купить ${counter} дроидов, не более 7-ми и у теб€ осталось 420`
  );
}